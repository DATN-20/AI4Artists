enum CanvasMode {
    BRUSH_MODE = 0,
    DRAG_MODE = 1,
    SELECT_MODE = 2,
    SHAPE_MODE = 3,
    ERASE_MODE = 4,
    CROP_MODE = 10,
}

export enum ShapeModeOptions {
    RECTANGLE_SHAPE = 1,
    CIRCLE_SHAPE = 2
}
  
export default CanvasMode